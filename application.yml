spring:
  application:
    name: ms-employee
  data:
    mongodb:
      host: localhost
      port: 27017
      database: cjavadatabase

server:
  port: 8083
 
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
            defaultZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost     
